import React from 'react'
import ServicesSection from '../components/Services/Services'


function Service() {
  return (
    <div>
     <ServicesSection />  {/* This is where the ServicesSection component is imported and rendered */}  {/* Replace ServicesSection with the actual component name */}  {/* Make sure the ServicesSection component is correctly defined and imported */}  {/* Replace 'ServicesSection' with the actual component name */}  {/* Make sure the ServicesSection component is correctly defined and imported */}  {/* Replace 'ServicesSection' with the actual component name */}  {/* Make sure the ServicesSection component is correctly defined and imported */}  
    </div>
    
  )
}

export default Service